# CPU-only note: base docker-compose.yml runs on CPU by default (ONNX CPUExecutionProvider).
# Use docker-compose.gpu.yml on GPU VMs with NVIDIA Container Toolkit installed.
# Usage (CPU VM): docker compose up -d --build
# Usage (GPU VM): docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d --build
services: {}
