[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = backend/tests
markers =
    integration: marks tests requiring GPU, MediaMTX, and test video (deselect with '-m "not integration"')
