# MediaMTX — accept RTMP push, re-publish as RTSP (latest schema)
rtmp: yes
rtmpAddress: :1935
rtsp: yes
rtspAddress: :8554

pathDefaults:
  source: publisher
  overridePublisher: yes

paths:
  cam01:
    source: publisher
  cam02:
    source: publisher
  cam03:
    source: publisher
